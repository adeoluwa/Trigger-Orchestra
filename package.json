{
  "name": "trigger-orchestra",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "dev:api": "turbo run dev --filter=@trigger-orchestra/api",
    "dev:web": "turbo run dev --filter=@trigger-orchestra/web"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
